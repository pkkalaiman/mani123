package com.xworkz.abstraction.interfaceapp;

public interface Bank { // interface class is 1st Step

/* it is an  INTERFACE class you have write public are not that's not care this interface class*/
	void Deposit(int amount);
	void Withdrawing(int amount);
	void CheckBalance();
	
}
